# Milk & Ink Frontend

Copy your React/Vite frontend files here.

Required files: `package.json`, `package-lock.json`, `vite.config.js`, `src/`, `index.html`

The `deploy-frontend.yml` workflow will build and deploy from this directory.
